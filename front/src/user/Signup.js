import React from 'react';
import Layout from '../core/Layout';
import {API} from '../config';

const Signup = () => (
  <Layout
    title="signup"
    description="Signup to Node Kaiser E-commerce" >

       {API} 
    </Layout>
);

export default Signup;
